// Marp CLI configuration.
// Auto-detected by `marp` when run from this directory, so the Shiki engine
// (which enables per-line spans for CSS line numbers) applies to every
// invocation — including `marp -s .` server mode.
export default {
  engine: './engine.mjs',
}
